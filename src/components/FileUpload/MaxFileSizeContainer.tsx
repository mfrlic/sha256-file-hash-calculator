import { styled } from "@mui/material";

const MaxFileSizeContainer = styled("div")(({ theme }) => ({
  fontSize: theme.typography.caption.fontSize,
  color: theme.palette.text.secondary,
}));

export default MaxFileSizeContainer;
